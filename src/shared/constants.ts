export const CHATGPT_URLS = [
  "https://chatgpt.com/*",
  "https://chat.openai.com/*",
];

export const DEFAULT_MAX_ROUNDS = 5;
export const POLL_INTERVAL_MS = 5000;    // Check research status every 5s
export const DEFAULT_SETTINGS = {
  glmApiKey: "",
  claudeApiKey: "",
  maxRounds: 5,
  autoMode: false,
};
